import React, { FC } from "react";
import { Map, InfoDrawer } from "../../component";

const Entry: FC = (props) => {
    return (
        <div>
            <Map />
            <InfoDrawer />
        </div>
    );
};

export default Entry;
